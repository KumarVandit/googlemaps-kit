# Dependencies & build
node_modules/
dist/
dist-server/
coverage/
*.tgz

# Local secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
cookies.txt
*.cookies
auth-session*

# Machine captures, probe dumps, browser profiles (device/session material)
.probe/
.cache/
.playwright/
chrome-profile/
tmp/
*.log
**/capture.log
**/cdp-*.json

# Product website — deploy locally via `cd site && npx vercel --prod` (not published)
site/

# Maintainer-only tooling (not part of the public repo)
scripts/archive/
scripts/capture-*.ts
scripts/analyze-*.ts
scripts/benchmark.ts
scripts/audit-quality.ts
scripts/registry-summary.ts
scripts/save-fixtures.ts
scripts/smoke-api-server.ts
scripts/verify-*.ts
!scripts/verify-all.ts
docs/internal/
examples/internal/

# Local AI tool state
.claude/
.cursor/
.agents/
AGENTS.md

# OS
.DS_Store
