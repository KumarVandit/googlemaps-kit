# Archived probe scripts

One-off ablation and shape-probing scripts used during reverse engineering.
They are kept for reference but are not wired in package.json.

Run directly: `tsx scripts/archive/probe-batchexecute.ts`

Active maintainer tooling lives in the parent `scripts/` directory (verify-*,
capture-*, benchmark, etc.).
